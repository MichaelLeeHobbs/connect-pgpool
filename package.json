{
  "name": "connect-pgpool",
  "version": "0.1",
  "dependencies": {
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "pg": "^8.4.1"
  },
  "scripts": {
    "build": "docker build -t connect-pgpool .",
    "build:start": "docker build -t connect-pgpool . && docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d",
    "start": "node src/index",
    "start:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d",
    "test": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 jest"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "jest": "^26.5.3",
    "node-fetch": "^2.6.1",
    "pkg": "^4.4.9"
  }
}
